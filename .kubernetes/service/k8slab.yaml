apiVersion: v1
kind: Service
metadata:
  name: k8slab
spec:
  type: NodePort
  selector:
    run: k8slab
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
